<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DemoApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DemoApp" parent="Base.Theme.DemoApp" />
    <style name="textStyle1">
        <item name="fontFamily">@font/font</item>
        <item name="android:textSize">@dimen/_20ssp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/black</item>
    </style>
    
    <style name="roundImageCorner">
        <item name="cornerSizeTopLeft">@dimen/_20ssp</item>
        <item name="cornerSizeTopRight">@dimen/_20ssp</item>
    </style>
</resources>